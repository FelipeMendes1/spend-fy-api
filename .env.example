# Perfil ativo (dev usa H2 em memória, prod usa PostgreSQL)
APP_PROFILE=dev

# JWT
JWT_SECRET=troque-este-valor-por-uma-chave-secreta-forte-em-producao
JWT_EXPIRATION=86400000

# Banco de dados (usado apenas com APP_PROFILE=prod)
DB_URL=jdbc:postgresql://localhost:5432/spendfy
DB_USERNAME=spendfy
DB_PASSWORD=spendfy123

# Docker Compose
POSTGRES_USER=spendfy
POSTGRES_PASSWORD=spendfy123
